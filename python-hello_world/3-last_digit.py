#!/usr/bin/python3
import random
number = random.randint(-10000, 10000)
# YOUR CODE HERE
last_digit = last_digit

if last_digit > 5:
    string = "greater than 5"
elif last_digit == 0:
    string = "0"
else:
    string = "less than 6 amd not 0"

    print("last digit of {} is {}".format(number, last_digit, string))
